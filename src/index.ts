import SERVER_URL_MAPPINGS from "./env/SERVER_URL_MAPPINGS"

import { CategoryModel, DebugPropertyImageUploadModel, PriceModel, PropertyModel, UserModel } from "./models"

export default {
    SERVER_URL_MAPPINGS,
    
    CategoryModel,
    DebugPropertyImageUploadModel,
    PriceModel,
    PropertyModel,
    UserModel,
};
